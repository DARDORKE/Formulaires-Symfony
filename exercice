Créez une entité User contenant les propriétés :

username / string / 255 / non nullable

password / string / 255 / non nullable

email / string / 255 / non nullable

birthdate / date / nullable

À partir de cette entité, créez un formulaire UserType en utilisant le composant maker, puis écrivez le code permettant
 de gérer la soumission du formulaire dans votre contrôleur.